#ifndef __HCF_BT_H
    #define __HCF_BT_H

   /* #include "esp_err.h"

    // Tipo da função callback que será chamada ao receber um caractere
    typedef void (*bt_server_recv_cb_t)(char data);

    // Inicializa o Bluetooth SPP Server
    esp_err_t bt_server_init(bt_server_recv_cb_t recv_cb);

    // Envia um caractere via Bluetooth
    esp_err_t bt_server_send_char(char data);*/

#endif